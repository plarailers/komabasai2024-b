import { Container } from "@mantine/core";
import { Layout } from "../components/Layout";

const Home: React.FC = () => {
  return (
    <Layout>
      <Container>hello</Container>
    </Layout>
  );
};

export default Home;
